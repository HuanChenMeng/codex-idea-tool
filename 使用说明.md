# Codex Provider Manager（Tauri 版）

## 使用顺序

### 第三方 Provider

1. 打开“第三方 Provider”，点击“新建 Provider”。
2. 填写 Provider ID、显示名称、模型 ID、Base URL、API Key 和协议。
3. 首次使用先保存，再获取模型列表；也可以直接手动填写模型 ID。
4. 点击“配置思考等级”，为每个模型分别选择支持档位与默认档位。
5. 测试连接并再次保存配置。
6. 完全退出所有 IDEA，在“启动配置”选择该 Provider 后点击“启动隔离 IDEA”。

### OpenAI 多账号

1. 打开“OpenAI 账号”，新增一个仅用于本机识别的账号名称。
2. 点击“登录”，在默认浏览器完成普通 OAuth；也可以导入该账号已有的 Codex `auth.json`。
3. 卡片状态会每 2.5 秒自动刷新，不需要重启本工具。
4. 状态显示“已登录”后，从账号卡片或“启动配置”页面启动 IDEA。

## 数据位置

- 第三方配置：`%LOCALAPPDATA%\JetBrains\CodexIdeaHome`
- OpenAI 独立账号：`%LOCALAPPDATA%\JetBrains\CodexIdeaProfiles`
- 完整日志：`%LOCALAPPDATA%\JetBrains\CodexIdeaHome\logs`
- 新版 Provider 索引：`%LOCALAPPDATA%\JetBrains\CodexIdeaHome\ci-providers.json`

API Key 会保存在 IDEA 专用配置中，不写入系统环境变量。只有通过本工具启动的 IDEA 才使用隔离配置；Codex 桌面客户端和普通快捷方式启动的 IDEA 不受影响。

## 备份

保存、覆盖和导入前会创建带时间戳的备份。“清理备份”会按原文件分别保留最近 5 份。“恢复备份”恢复最近一次第三方 `config.toml` 备份。
